<username>/SimpleMeteorApp:
  PreInstall:
    # Install meteor
    - 'sudo curl https://install.meteor.com/ | sh'
  PkgInclude:
    - '*'
  PkgExclude:
    - '/.meteor/local/'
  Exec:
    - 'meteor run'
